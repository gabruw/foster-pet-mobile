//#region Imports

import { StyleSheet } from 'react-native';

//#endregion

const useStyles = () =>
    StyleSheet.create({
        right: {
            marginRight: 10
        }
    });

export default useStyles;
