//#region Imports

import { StyleSheet } from 'react-native';

//#endregion

const useStyles = () =>
    StyleSheet.create({
        left: {
            marginLeft: 8
        }
    });

export default useStyles;
