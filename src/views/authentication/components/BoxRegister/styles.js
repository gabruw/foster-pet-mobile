//#region Imports

import ALIGN from 'assets/styles/align';
import { StyleSheet } from 'react-native';

//#endregion

const useStyles = () =>
    StyleSheet.create({
        arrow: {
            marginTop: 20,
            marginLeft: 20
        }
    });

export default useStyles;
