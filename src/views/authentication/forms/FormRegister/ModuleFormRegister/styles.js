//#region Imports

import { StyleSheet } from 'react-native';

//#endregion

const useStyles = () =>
    StyleSheet.create({
        container: {
            marginTop: 15,
            marginBottom: 5
        }
    });

export default useStyles;
