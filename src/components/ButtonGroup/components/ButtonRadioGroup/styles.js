//#region Imports

import { StyleSheet } from 'react-native';

//#endregion

const useStyles = () =>
    StyleSheet.create({
        radioGroup: {
            display: 'none'
        }
    });

export default useStyles;
