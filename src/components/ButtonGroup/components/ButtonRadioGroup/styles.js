//#region Imports

import { StyleSheet } from 'react-native';
import COLOR from 'assets/styles/color';

//#endregion

const useStyles = () =>
    StyleSheet.create({
        radioGroup: {
            display: 'none'
        }
    });

export default useStyles;
